pipeline {
    agent any

    tools {nodejs "node"}

    stages {
        stage('Cypress Parallel Test Suite') {
            steps {
                git branch: 'chapter5', url: 'https://github.com/esdeguzman/todoer.git'
                bat 'npm install'
                bat 'npm update'
                bat 'npm run %script%'
            }
        }
    }
}