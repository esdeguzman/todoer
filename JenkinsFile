pipeline {
    agent any

    tools {nodejs "node"}

    stages {
        stage('Cypress Parallel Test Suite') {
            steps {
                git url: 'https://github.com/esdeguzman/todoer.git'
                bat 'npm install'
                bat 'npm update'
                bat 'npm run cypress'
            }
        }
    }
}